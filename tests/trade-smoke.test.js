import assert from"node:assert/strict";import{CONFIG}from"../js/core/config.js";import{ResourceService}from"../js/domain/resource-service.js";import{TradeService}from"../js/domain/trade-service.js";
const r=new ResourceService(),t=new TradeService(r),s={year:1,day:CONFIG.FIRST_TRADE_DAY,status:"playing",speed:2,inventory:{},company:{cash:10000,earn:0},trade:{active:false,nextArrivalDay:CONFIG.FIRST_TRADE_DAY,visits:0,returnSpeed:1,arrivedAt:null}};const tile={type:"industry",resourceId:"iron",name:"Iron Ore"};t.store(s,tile,1000);assert.equal(t.stock(s)[0].amount,1000);assert.ok(t.shouldArrive(s));assert.ok(t.arrive(s));assert.equal(s.speed,0);const cash=s.company.cash;assert.ok(t.sell(s,"industry:iron",500).ok);assert.ok(s.company.cash>cash&&s.company.earn>0);const before=s.inventory["industry:iron"].amount,buyCash=s.company.cash;assert.ok(t.buy(s,"industry:iron",100).ok);assert.ok(s.inventory["industry:iron"].amount>before&&s.company.cash<buyCash);assert.ok(t.buyPrice("industry")>t.sellPrice("industry"));assert.ok(t.depart(s));assert.equal(s.speed,2);assert.equal(t.daysUntilArrival(s),CONFIG.TRADE_INTERVAL_DAYS);console.log("MineIT trade smoke test passed");
