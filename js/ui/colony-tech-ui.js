import { clamp, formatMoney, formatNumber } from "../core/utils.js";
import { CONFIG } from "../core/config.js";

export class ColonyTechUIMixin {
  supplyDaysLabel(days){if(days===null||days===undefined)return"SURPLUS";if(days<=0)return"EMPTY";return`${Math.max(1,Math.ceil(days))}d`}
  supplyRiskClass(days,supply){if(supply<.4||days!==null&&days<=CONFIG.SUPPLY_CRITICAL_DAYS)return"bad";if(supply<.9||days!==null&&days<=CONFIG.SUPPLY_WARN_DAYS)return"warn";return"good";}
  colonyPanel(){
    this.onRecalculate?.();const s=this.state,m=s.metrics,c=s.colony,dead=s.status==="dead",h=this.colony.canExpandHousing(s),i=this.colony.canExpandIndustry(s),pct=v=>`${Math.round(clamp(v,0,1)*100)}%`,relocation=this.colony.relocationCost(s),completed=s.contract.completed&&!s.contract.ended,staffReq=m.industryPopulationRequired||this.colony.industryPopulationRequirement(c.industryLevel),nextStaff=this.colony.industryPopulationRequirement(c.industryLevel+1),survival=m.survivalSupply??1,survivalLabel=dead?"COLONY LOST":survival>=.9?"STABLE":survival>=.7?"STRAINED":survival>=.4?"CRITICAL":"COLLAPSE";
    const status=dead?"COLONY LOST":s.status==="liability"?"LIABILITY — SUPPORT ONLY":s.status==="holdover"?"CONTRACT COMPLETE — HOLDOVER":completed?"RENEWED CONTRACT":"ACTIVE CONTRACT",foodCls=this.supplyRiskClass(m.foodDays,m.foodSupply??1),fuelCls=this.supplyRiskClass(m.fuelDays,m.fuelSupply??1),oreCls=this.supplyRiskClass(m.oreDays,m.oreSupply??1),canAbandonDead=dead&&s.portfolio.colonies.length>1;
    if(dead){this.open(`${s.contract.colonyName||"Colony"} — LOST`,`<article class="card"><h3 class="bad">COLONY LOST</h3><p>The population reached zero on Y${s.contract.deathYear||s.year} D${s.contract.deathDay||s.day}. The charter failed and all extraction and Industry have stopped permanently.</p><div class="effect warn">This colony no longer consumes resources or operating cash. Corporation reputation was reduced when it was lost.</div></article><div class="grid2" style="margin-top:7px"><div class="metric"><small>Population</small><strong>0</strong></div><div class="metric"><small>Final Industry level</small><strong>L${c.industryLevel}</strong></div><div class="metric"><small>Stored Food</small><strong>${formatNumber(m.foodStock||0)}</strong></div><div class="metric"><small>Stored Fuel</small><strong>${formatNumber(m.fuelStock||0)}</strong></div></div><div class="colony-management"><button data-all-colonies>VIEW ALL COLONIES</button>${canAbandonDead?`<button data-abandon-dead class="bad">ABANDON DEAD COLONY</button>`:""}</div>`);this.modal.querySelector("[data-all-colonies]").onclick=()=>this.coloniesPanel();const abandon=this.modal.querySelector("[data-abandon-dead]");if(abandon)abandon.onclick=()=>this.onRemoveColony?.("abandon-dead");return;}
    this.open(`${s.contract.colonyName||"Colony"} — Tier ${s.contract.colonyTier}`,`<article class="card"><h3>${status}</h3><p>${s.contract.environment}<br>${s.contract.hazard}</p><div class="effect">Colony support: ${s.contract.supportSystem} • Environmental load ×${Number(s.contract.supportLoad||1).toFixed(2)}</div><div class="effect">Tech access: ${s.contract.techAccess==="direct"?"Direct corporate link":"Trade ship only"}</div><div class="effect ${survival>=.9?"good":survival>=.4?"warn":"bad"}">SURVIVAL: ${survivalLabel} • ${pct(survival)} effective Food/Power supply</div>${(m.lastDeaths||0)>0?`<div class="effect bad">Deaths last day: ${formatNumber(m.lastDeaths)}</div>`:""}${s.contract.ended?`<div class="requirement locked">Mining rights ended. Resource extraction and new site development are disabled.</div>`:""}</article><div class="grid2" style="margin-top:6px"><div class="metric"><small>Population / Housing</small><strong>${formatNumber(s.pop)} / ${formatNumber(c.housingCapacity)}</strong></div><div class="metric"><small>Industry</small><strong>L${c.industryLevel} • ${formatNumber(m.industry)}</strong></div><div class="metric"><small>Industry staffing</small><strong>${pct(m.industryStaffFactor??1)} • ${formatNumber(staffReq)} pop for 100%</strong></div><div class="metric"><small>Power</small><strong>${formatNumber(m.powerDemand)} / ${formatNumber(m.powerCapacity)}</strong></div><div class="metric"><small>Operating cash cost</small><strong>${formatMoney(m.operatingCost)}/d</strong></div><div class="metric"><small>Support load</small><strong>×${Number(m.supportLoad||s.contract.supportLoad||1).toFixed(2)}</strong></div></div><div class="need-grid"><div class="metric need ${foodCls}"><small>Food • ${this.supplyDaysLabel(m.foodDays)}</small><strong>${formatNumber(m.foodDemand)}/d • ${pct(m.foodSupply)}</strong></div><div class="metric need ${fuelCls}"><small>Fuel • ${this.supplyDaysLabel(m.fuelDays)}</small><strong>${formatNumber(m.fuelDemand)}/d • ${pct(m.fuelSupply)}</strong></div><div class="metric need ${oreCls}"><small>Ore • ${this.supplyDaysLabel(m.oreDays)}</small><strong>${formatNumber(m.oreDemand)}/d • ${pct(m.oreSupply)}</strong></div><div class="metric need ${m.powerFactor>=.9?"good":m.powerFactor>=.4?"warn":"bad"}"><small>Power available</small><strong>${pct(m.powerFactor)}</strong></div></div><article class="card" style="margin-top:7px"><h3>${c.emergencyMode?"EMERGENCY MODE ACTIVE":"EMERGENCY MODE"}</h3><p>${c.emergencyMode?"Industry and Build/Ore extraction are shut down. Life support is running at reduced power demand while Food and Fuel production continue.":"Use only when survival stocks are collapsing. It preserves life support by shutting down Industry and non-essential extraction."}</p><button data-emergency class="${c.emergencyMode?"warn":""}">${c.emergencyMode?"EXIT EMERGENCY MODE":"ENTER EMERGENCY MODE"}</button></article><div class="grid2" style="margin-top:7px"><button data-housing ${h.ok?"":"disabled"}>BUILD HOUSING<br><span class="tiny">${formatMoney(h.cash||this.colony.housingCashCost(s))} + ${formatNumber(h.build||this.colony.housingBuildCost(s))} Build</span></button><button data-industry ${i.ok?"":"disabled"}>EXPAND INDUSTRY<br><span class="tiny">${formatMoney(i.cash||this.colony.industryCashCost(s))} + ${formatNumber(i.build||this.colony.industryBuildCost(s))} Build • ${formatNumber(i.populationRequired||nextStaff)} pop for full output</span></button></div>${h.ok?"":`<div class="requirement locked">Housing: ${h.reason}</div>`}${i.ok?"":`<div class="requirement locked">Industry: ${i.reason}</div>`}<div class="colony-management">${s.status==="holdover"?`<button data-renew>RENEW ${CONFIG.RENEWAL_YEARS} YEARS<br><span class="tiny">${formatMoney(this.contracts.renewalFee(s))}</span></button>`:""}${completed?`<button data-end>END / RETURN CONTRACT</button>`:""}${this.contracts.canOpenAdditional(s)?`<button data-new>OPEN NEW COLONY</button>`:""}${s.status==="liability"?`<button data-relocate ${s.company.cash>=relocation&&s.portfolio.colonies.length>1?"":"disabled"}>RELOCATE POPULATION<br><span class="tiny">${formatMoney(relocation)}</span></button>`:""}<button data-all-colonies>VIEW ALL COLONIES</button></div>`);
    const emergency=this.modal.querySelector("[data-emergency]");if(emergency)emergency.onclick=()=>{c.emergencyMode=!c.emergencyMode;this.onRecalculate?.();this.repo.save(s);this.toast(c.emergencyMode?"Emergency Mode active: Industry and non-essential extraction stopped.":"Emergency Mode ended: normal operations resumed.");this.colonyPanel()};
    const hb=this.modal.querySelector("[data-housing]");if(hb)hb.onclick=()=>{const r=this.colony.expandHousing(s);if(r.ok){this.onRecalculate?.();this.repo.save(s);this.toast("Housing capacity expanded. Population does not arrive automatically; use a corporate ship transfer.");this.colonyPanel()}else this.toast(r.reason)};
    const ib=this.modal.querySelector("[data-industry]");if(ib)ib.onclick=()=>{const r=this.colony.expandIndustry(s);if(r.ok){this.onRecalculate?.();this.repo.save(s);this.toast(`Industry expanded to L${s.colony.industryLevel}. Full staffing needs ${formatNumber(this.colony.industryPopulationRequirement(s.colony.industryLevel))} population.`);this.colonyPanel()}else this.toast(r.reason)};
    const renew=this.modal.querySelector("[data-renew]");if(renew)renew.onclick=()=>{const r=this.contracts.renew(s);if(r.ok){this.onRecalculate?.();this.repo.save(s);this.toast(`Contract renewed for ${r.years} years.`);this.colonyPanel()}else this.toast(r.reason)};
    const end=this.modal.querySelector("[data-end]");if(end)end.onclick=()=>this.endContractDialog();const newColony=this.modal.querySelector("[data-new]");if(newColony)newColony.onclick=()=>this.contractBoard();const all=this.modal.querySelector("[data-all-colonies]");if(all)all.onclick=()=>this.coloniesPanel();const relocate=this.modal.querySelector("[data-relocate]");if(relocate)relocate.onclick=()=>this.onRelocateColony?.();
  }
  techEffect(category,tech){if(category==="power")return`Power ${formatNumber(tech.powerCapacity)} • Population cap ${formatNumber(tech.populationCap)} • Industry L${tech.industryCap} • Fuel intensity ${tech.fuelIntensity.toFixed(3)}×`;if(category==="food")return`Natural production ×${tech.productionMultiplier.toFixed(2)}${tech.syntheticFood?` • Sealed food ${formatNumber(tech.syntheticFood)}/day`:""}`;const unlocks=this.resources.catalog().filter(r=>r.miningLevel===tech.level&&!r.manufactured).map(r=>r.name);return`Extraction ×${tech.extractionMultiplier.toFixed(2)}${unlocks.length?` • Unlocks: ${unlocks.join(", ")}`:""}`;}
  tech(){
    this.onRecalculate?.();if(this.showFutureTech===undefined)this.showFutureTech=true;const access=this.technology.canAccessStore(this.state),cats=["power","food","mining"],labels={power:"POWER",food:"FOOD PRODUCTION",mining:"MINING"};
    const paths=cats.map(cat=>{const level=this.technology.level(this.state,cat),items=this.technology.tree(cat).filter(t=>this.showFutureTech||t.level<=level);return`<section class="tech-path"><div class="tech-path-header"><strong>${labels[cat]}</strong><span>L${level}/10</span></div><div class="tech-roadmap">${items.map(t=>{const owned=t.level<level,current=t.level===level,next=t.level===level+1,future=t.level>level+1,stateClass=owned?"owned":current?"current":next?"next":"future",stateLabel=owned?"OWNED":current?"CURRENT":next?"NEXT":"LOCKED";return`<article class="tech-roadmap-card ${stateClass}"><div class="tech-roadmap-level">L${t.level}</div><div class="tech-roadmap-copy"><div class="tech-roadmap-title"><strong>${t.name}</strong><span>${stateLabel}</span></div><p>${t.description}</p><div class="effect">${this.techEffect(cat,t)}</div>${future?`<div class="requirement">Requires ${labels[cat]} L${t.level-1}</div>`:""}</div><div class="tech-roadmap-action">${next?`<button data-tech-cat="${cat}" ${!access||this.state.company.cash<t.cost?"disabled":""}>${formatMoney(t.cost)}</button>`:current?`<span>ACTIVE</span>`:owned?`<span>✓</span>`:`<span>🔒</span>`}</div></article>`;}).join("")}</div></section>`;}).join("");
    this.open("Corporate Technology",`<div class="tech-toolbar"><article class="card"><h3>${access?"CORPORATE SYSTEMS ONLINE":"CORPORATE SYSTEMS UNAVAILABLE"}</h3><p>${this.technology.accessText(this.state)}</p><div class="effect warn">Advanced licences are major corporation-scale investments; late levels cost tens or hundreds of millions.</div></article><button data-tech-toggle>${this.showFutureTech?"HIDE FUTURE TECH":"SHOW FUTURE TECH"}</button></div><div class="tech-tree">${paths}</div>`);this.modal.querySelector("[data-tech-toggle]").onclick=()=>{this.showFutureTech=!this.showFutureTech;this.tech()};this.modal.querySelectorAll("[data-tech-cat]").forEach(b=>b.onclick=()=>{const r=this.technology.buy(this.state,b.dataset.techCat);if(r.ok){this.onRecalculate?.();this.repo.save(this.state);this.toast(`${r.tech.name} licensed permanently.`);this.tech()}else this.toast(r.reason)});
  }
  goals(){const score=this.contracts.score(this.state),g=this.state.contract.goals;this.open("Contract Goals",`<article class="card"><h3>${this.state.contract.colonyName} • COLONY TIER ${this.state.contract.colonyTier}</h3><p>${this.state.contract.environment}<br>${this.state.contract.hazard}</p></article><div class="grid2" style="margin-top:6px"><div class="metric"><small>Food production</small><strong>${formatNumber(this.state.metrics.food)} / ${formatNumber(g.food)}</strong></div><div class="metric"><small>Industry capability</small><strong>${formatNumber(this.state.metrics.industry)} / ${formatNumber(g.industry)}</strong></div><div class="metric"><small>Population</small><strong>${formatNumber(this.state.pop)} / ${formatNumber(g.pop)}</strong></div><div class="metric"><small>Contract profit</small><strong>${formatMoney(score.profit)}</strong></div><div class="metric"><small>Local revenue</small><strong>${formatMoney(score.revenue)}</strong></div><div class="metric"><small>Colony costs</small><strong>${formatMoney(score.costs)}</strong></div></div><div class="card" style="margin-top:5px"><h3>Performance bands</h3><p>Bronze: pass all objectives. Silver: ${formatMoney(this.state.contract.bands.silver)} profit. Gold: ${formatMoney(this.state.contract.bands.gold)} plus 120% objectives. Platinum: ${formatMoney(this.state.contract.bands.plat)} plus 150% objectives.</p></div>`);}
}
