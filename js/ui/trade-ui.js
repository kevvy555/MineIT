import{CONFIG}from"../core/config.js";import{formatMoney,formatNumber}from"../core/utils.js";
export class TradeUI{constructor({state,trade,repo,ui}){Object.assign(this,{state,trade,repo,ui});this.b=document.querySelector("#tradeBtn");this.b.onclick=()=>state.trade.active?this.open():this.status();document.querySelectorAll("[data-speed]").forEach(x=>x.onclick=()=>{if(state.trade.active&&+x.dataset.speed>0){ui.toast("Corporate ship is docked. Depart before resuming time.");return}state.speed=+x.dataset.speed;ui.syncSpeed();this.render()})}price(v){return v<10?`£${v.toFixed(2)}`:formatMoney(v)}render(){let s=document.querySelector("#income");if(s)s.textContent=formatMoney(this.trade.stockValue(this.state));this.b.classList.toggle("trade-live",this.state.trade.active);this.b.textContent=this.state.trade.active?"TRADE!":`SHIP ${formatNumber(this.trade.daysUntilArrival(this.state))}d`;document.querySelectorAll("[data-speed]").forEach(x=>x.disabled=this.state.trade.active&&+x.dataset.speed>0)}status(){this.ui.open("Corporate Trade Ship",`<article class="card"><h3>NEXT VISIT IN ${formatNumber(this.trade.daysUntilArrival(this.state))} DAYS</h3><p>A corporate vessel arrives every ${CONFIG.TRADE_INTERVAL_DAYS} days (six game months). Collected resources stay in storage until a ship docks.</p><div class="grid2"><div class="metric"><small>Stored stock value</small><strong>${formatMoney(this.trade.stockValue(this.state))}</strong></div><div class="metric"><small>Completed visits</small><strong>${this.state.trade.visits}</strong></div></div></article>`)}open(k=null){if(!this.state.trade.active){this.status();return}let stock=this.trade.stock(this.state),cat=this.trade.catalog(),sel=cat.find(x=>x.key===k)||cat[0],bp=this.trade.buyPrice(sel.type);this.ui.open("Corporate Trade Ship — DOCKED",`<div class="trade-summary"><div class="metric"><small>Cash</small><strong>${formatMoney(this.state.company.cash)}</strong></div><div class="metric"><small>Stock value</small><strong>${formatMoney(this.trade.stockValue(this.state))}</strong></div><div class="metric"><small>Next visit</small><strong>${CONFIG.TRADE_INTERVAL_DAYS}d after departure</strong></div></div><section class="trade-section"><h3>SELL COLLECTED STOCK</h3>${stock.length?`<div class="trade-table">${stock.map(e=>`<div class="trade-row"><strong>${e.name}</strong><span>${formatNumber(e.amount)}</span><span>${this.price(this.trade.sellPrice(e.type))}/u</span><div class="trade-actions"><button data-q="${e.key}">25%</button><button data-a="${e.key}">ALL</button></div></div>`).join("")}</div>`:`<div class="card"><p>No stored resources available to sell.</p></div>`}</section><section class="trade-section"><h3>BUY FROM CORPORATION</h3><div class="trade-buy"><select data-r>${cat.map(e=>`<option value="${e.key}" ${e.key===sel.key?"selected":""}>${e.category} — ${e.name}</option>`).join("")}</select><div class="effect">Corporate price: ${this.price(bp)} per unit</div><div class="trade-buy-buttons"><button data-buy="100" ${this.state.company.cash<bp*100?"disabled":""}>BUY 100</button><button data-buy="1000" ${this.state.company.cash<bp*1000?"disabled":""}>BUY 1K</button><button data-buy="10000" ${this.state.company.cash<bp*10000?"disabled":""}>BUY 10K</button></div><div class="trade-note">Purchased stock goes into storage and does not count as local Food or Industry production.</div></div></section><div class="trade-footer"><button data-all ${stock.length?"":"disabled"}>SELL ALL STOCK</button><button data-depart class="warn">SHIP DEPARTS</button></div>`);let m=this.ui.modal;m.querySelector("[data-r]").onchange=e=>this.open(e.target.value);m.querySelectorAll("[data-q]").forEach(b=>b.onclick=()=>{let e=this.state.inventory[b.dataset.q],r=this.trade.sell(this.state,b.dataset.q,(e?.amount||0)*.25);if(r.ok){this.ui.toast(`Sold ${formatNumber(r.qty)} for ${formatMoney(r.revenue)}.`);this.open(sel.key)}});m.querySelectorAll("[data-a]").forEach(b=>b.onclick=()=>{let e=this.state.inventory[b.dataset.a],r=this.trade.sell(this.state,b.dataset.a,e?.amount||0);if(r.ok){this.ui.toast(`Sold ${formatNumber(r.qty)} for ${formatMoney(r.revenue)}.`);this.open(sel.key)}});m.querySelectorAll("[data-buy]").forEach(b=>b.onclick=()=>{let r=this.trade.buy(this.state,sel.key,+b.dataset.buy);if(r.ok){this.ui.toast(`Bought ${formatNumber(r.qty)} ${r.entry.name}.`);this.open(sel.key)}else this.ui.toast(r.reason)});m.querySelector("[data-all]").onclick=()=>{let r=this.trade.sellAll(this.state);if(r.ok){this.ui.toast(`Sold all stock for ${formatMoney(r.revenue)}.`);this.open(sel.key)}};m.querySelector("[data-depart]").onclick=()=>{this.trade.depart(this.state);m.classList.add("hidden");this.ui.syncSpeed();this.render();this.repo.save(this.state);this.ui.toast(`Trade ship departed • next visit in ${CONFIG.TRADE_INTERVAL_DAYS} days.`)}}}
